# Virasat Wealth

Django advisory website for Virasat Wealth with user authentication, consultation requests, lead export, and production deployment configuration.

## Local Development

```bash
python3 -m pip install -r requirements.txt
python3 manage.py migrate
python3 manage.py runserver
```

## Deployment

This project includes `render.yaml` for deployment on Render.
